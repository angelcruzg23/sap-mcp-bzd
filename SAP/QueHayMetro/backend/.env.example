# X (Twitter) API
X_BEARER_TOKEN=tu_bearer_token_aqui

# Claude API (Anthropic)
ANTHROPIC_API_KEY=tu_api_key_aqui

# Servidor
PORT=3000

# Modo: "mock" para pruebas sin APIs, "live" para producción
MODE=mock
